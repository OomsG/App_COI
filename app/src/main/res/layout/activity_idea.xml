<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".activities.IdeaActivity">

    <include
            layout="@layout/toolbar_layout" android:id="@+id/IdeaToolbar"/>
    <TextView
            android:text="Titel!"
            android:layout_width="wrap_content"
            android:layout_height="8dp"
            android:id="@+id/IdeationTitle"
            app:layout_constraintTop_toBottomOf="@+id/IdeaToolbar"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="8dp" app:layout_constraintEnd_toEndOf="parent" android:layout_marginEnd="8dp"/>
    <fragment
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:name="be.kdg.cityofideas.fragments.IdeaFragment"
            android:id="@+id/IdeaFragment"
            app:layout_constraintTop_toBottomOf="@+id/IdeationTitle"
            android:layout_marginBottom="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="8dp"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginEnd="8dp"/>


</android.support.constraint.ConstraintLayout>